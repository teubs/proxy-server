name: 🚀 Proxy Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🧱 Build & recreate container with Docker Compose
        run: docker compose up -d --build

      - name: 🧹 Cleanup old images
        run: docker image prune -f
